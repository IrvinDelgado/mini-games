export type Position = {
  x: number;
  y: number;
};

export enum DIRECTION {
  UP = "UP",
  RIGHT = "RIGHT",
  DOWN = "DOWN",
  LEFT = "LEFT",
}

export enum KEYBOARD {
  UP = "ArrowUp",
  RIGHT = "ArrowRight",
  DOWN = "ArrowDown",
  LEFT = "ArrowLeft",
}
